dependencies:
    override:
        - export
        - env
        - echo "bash source ${BASH_SOURCE[@]}"
        - cat "${BASH_SOURCE[0]}"
test:
    override:
        - export
        - env
        - echo "bash source ${BASH_SOURCE[@]}"
        - cat "${BASH_SOURCE[0]}"
